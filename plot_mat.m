%---------------------------------------------------------------------------
% plot_mat.m
%---------------------------------------------------------------------------

% plots a matrix


function plot_mat(matrix);

  R=abs(matrix);

  maxR=max(max(R));
  R=128*(1-(R/maxR).*0.5);

  image(R);
  colormap(gray(128));
  axis square


%---------------------------------------------------------------------------
